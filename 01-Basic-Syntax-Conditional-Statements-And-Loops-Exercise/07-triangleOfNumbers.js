function triangleOfNumbers(n) {
    let temp = '';
    for (let i = 1; i <= n; i++) {
        console.log(`${i} `.repeat(i));
    }
}

triangleOfNumbers(3);
triangleOfNumbers(5);
triangleOfNumbers(6);
