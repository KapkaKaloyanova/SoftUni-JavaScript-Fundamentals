function sumFirstAndLastArrElem(input) {
    let firstElement = Number(input[0]);
    let lastElement = Number(input[input.length - 1]);
    console.log(firstElement + lastElement);
}
sumFirstAndLastArrElem([20, 30, 40]);
sumFirstAndLastArrElem([10, 17, 22, 33]);
sumFirstAndLastArrElem([11, 58, 69]);
